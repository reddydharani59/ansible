- name: conditions in ansible
  hosts: localhost
  become: yes
  tasks:
  - name: check user exists or not
    ansible.builtin.debug: id expense
    ignore_errors: true
  - name: say hello
    ansible.builtin.debug:
      msg: "hello"


   