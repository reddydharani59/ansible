- name: variables from inventory
  hosts: localhost
  tasks:
  - name: print the information
    ansible.builtin.debug:
      msg: "hi,learning {{course}}, from trainer {{trainer}} and duration is {{duration}}"
